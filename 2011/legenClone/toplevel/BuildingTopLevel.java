package legenClone.toplevel;

import legenClone.core.X;

/** Top-level strategy for auras. */
public class BuildingTopLevel {
  public static final void run() {
    while (true) {
      X.yield();
    }
  }
}
